import AppShell from '../../components/AppShell';export default function Settings(){return <AppShell title="Settings"><div className="card"><h3>Administration</h3><p>Create login users in Supabase Dashboard → Authentication → Users. Add the same user UUID to an employee record’s <code>auth_user_id</code> field.</p><p>Department, designation and shift masters are included in the database schema and can be extended by the developer.</p></div></AppShell>}
